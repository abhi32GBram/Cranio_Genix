// src/components/Footer.tsx

import React from 'react';

const Footer: React.FC = () => {
    return (
        <footer>
            <p>For medical use only. Always consult with a healthcare professional.</p>
        </footer>
    );
};

export default Footer;